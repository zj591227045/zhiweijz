version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:${POSTGRES_IMAGE_VERSION}
    container_name: ${PROJECT_NAME}-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    command: >
      postgres
      -c max_connections=200
      -c shared_buffers=256MB
      -c effective_cache_size=1GB
      -c maintenance_work_mem=64MB
      -c checkpoint_completion_target=0.9
      -c wal_buffers=16MB
      -c default_statistics_target=100
      -c random_page_cost=1.1
      -c effective_io_concurrency=200
      -c work_mem=4MB
      -c min_wal_size=1GB
      -c max_wal_size=4GB
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_EXTERNAL_PORT}:5432"
    networks:
      - zhiweijz-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # 后端服务
  backend:
    image: zj591227045/zhiweijz-backend:${BACKEND_IMAGE_VERSION}
    container_name: ${PROJECT_NAME}-backend
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV}
      DOCKER_ENV: ${DOCKER_ENV}
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
      JWT_SECRET: ${JWT_SECRET}
      PORT: ${BACKEND_PORT}
      # 微信公众号配置
      WECHAT_APP_ID: ${WECHAT_APP_ID}
      WECHAT_APP_SECRET: ${WECHAT_APP_SECRET}
      WECHAT_TOKEN: ${WECHAT_TOKEN}
      WECHAT_ENCODING_AES_KEY: ${WECHAT_ENCODING_AES_KEY}
      WECHAT_ENV: ${WECHAT_ENV}
      WECHAT_DEV_APP_ID: ${WECHAT_DEV_APP_ID}
      WECHAT_DEV_APP_SECRET: ${WECHAT_DEV_APP_SECRET}
      WECHAT_DEV_TOKEN: ${WECHAT_DEV_TOKEN}
      # 注意：S3存储配置已移至管理后台数据库配置
      # 如需配置对象存储，请在应用启动后通过管理后台进行配置
      FILE_MAX_SIZE: ${FILE_MAX_SIZE}
      FILE_ALLOWED_TYPES: ${FILE_ALLOWED_TYPES}
      # RevenueCat支付配置
      REVENUECAT_WEBHOOK_SECRET: ${REVENUECAT_WEBHOOK_SECRET}
      REVENUECAT_REST_API_KEY: ${REVENUECAT_REST_API_KEY}
      # 会员系统配置
      ENABLE_MEMBERSHIP_SYSTEM: ${ENABLE_MEMBERSHIP_SYSTEM}
      ENABLE_ACCOUNTING_POINTS_SYSTEM: ${ENABLE_ACCOUNTING_POINTS_SYSTEM}
      MEMBERSHIP_MONTHLY_POINTS: ${MEMBERSHIP_MONTHLY_POINTS}
      DEFAULT_MEMBER_TYPE: ${DEFAULT_MEMBER_TYPE}
      # 版本管理系统配置
      ENABLE_VERSION_MANAGEMENT: ${ENABLE_VERSION_MANAGEMENT}
      VERSION_CHECK_API_ENABLED: ${VERSION_CHECK_API_ENABLED}
      VERSION_CHECK_INTERVAL: ${VERSION_CHECK_INTERVAL}
      FORCE_UPDATE_GRACE_PERIOD: ${FORCE_UPDATE_GRACE_PERIOD}
      UPDATE_NOTIFICATION_ENABLED: ${UPDATE_NOTIFICATION_ENABLED}
      # 邮件服务配置
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      # AI服务配置
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    ports:
      - "${BACKEND_PORT}:${BACKEND_PORT}"
    networks:
      - zhiweijz-network
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -f http://localhost:${BACKEND_PORT}/api/health || curl -f http://localhost:${BACKEND_PORT}/"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 40s

  # 前端服务
  frontend:
    image: zj591227045/zhiweijz-frontend:${FRONTEND_IMAGE_VERSION}
    container_name: ${PROJECT_NAME}-frontend
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV}
      DOCKER_ENV: ${DOCKER_ENV}
      IS_MOBILE_BUILD: "false"
      PORT: ${FRONTEND_PORT}
      # 应用版本信息
      NEXT_PUBLIC_APP_VERSION: ${NEXT_PUBLIC_APP_VERSION}
      NEXT_PUBLIC_BUILD_NUMBER: ${NEXT_PUBLIC_BUILD_NUMBER}
      # 构建模式配置
      BUILD_MODE: ${BUILD_MODE}
      NEXT_PUBLIC_IS_MOBILE: ${NEXT_PUBLIC_IS_MOBILE}
      # API配置
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
      NEXT_PUBLIC_DEBUG_API_BASE_URL: ${NEXT_PUBLIC_DEBUG_API_BASE_URL}
      NEXT_PUBLIC_OFFICIAL_WEBSITE: ${NEXT_PUBLIC_OFFICIAL_WEBSITE}
      # 版本管理配置
      NEXT_PUBLIC_ENABLE_VERSION_CHECK: ${NEXT_PUBLIC_ENABLE_VERSION_CHECK}
      NEXT_PUBLIC_VERSION_CHECK_INTERVAL: ${NEXT_PUBLIC_VERSION_CHECK_INTERVAL}
      NEXT_PUBLIC_AUTO_VERSION_CHECK: ${NEXT_PUBLIC_AUTO_VERSION_CHECK}
      # 控制台日志管理配置
      NEXT_PUBLIC_ENABLE_CONSOLE_LOGS: ${NEXT_PUBLIC_ENABLE_CONSOLE_LOGS}
      NEXT_PUBLIC_LOG_LEVEL: ${NEXT_PUBLIC_LOG_LEVEL}
      NEXT_PUBLIC_PRESERVE_ERROR_LOGS: ${NEXT_PUBLIC_PRESERVE_ERROR_LOGS}
      NEXT_PUBLIC_ALLOW_RUNTIME_LOG_CONTROL: ${NEXT_PUBLIC_ALLOW_RUNTIME_LOG_CONTROL}
      # RevenueCat支付配置
      NEXT_PUBLIC_REVENUECAT_API_KEY: ${NEXT_PUBLIC_REVENUECAT_API_KEY}
      # 其他配置
      NEXT_TELEMETRY_DISABLED: ${NEXT_TELEMETRY_DISABLED}
    networks:
      - zhiweijz-network
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -s -o /dev/null -w '%{http_code}' http://localhost:${FRONTEND_PORT}/ | grep -E '^[23]'"]
      interval: 25s
      timeout: 10s
      retries: 4
      start_period: 60s

  # MinIO 对象存储服务
  minio:
    image: minio/minio:${MINIO_IMAGE_VERSION}
    container_name: ${PROJECT_NAME}-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BROWSER_REDIRECT_URL: ${MINIO_BROWSER_REDIRECT_URL}
    command: server /data --console-address ":9001"
    ports:
      - "${MINIO_API_PORT}:9000"
      - "${MINIO_CONSOLE_PORT}:9001"
    volumes:
      - minio_data:/data
    networks:
      - zhiweijz-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
      start_period: 30s

  # Nginx 反向代理
  nginx:
    image: zj591227045/zhiweijz-nginx:${NGINX_IMAGE_VERSION}
    container_name: ${PROJECT_NAME}-nginx
    restart: unless-stopped
    environment:
      PROJECT_NAME: ${PROJECT_NAME}
      BACKEND_PORT: ${BACKEND_PORT}
      FRONTEND_PORT: ${FRONTEND_PORT}
      EXTERNAL_DOMAIN: ${EXTERNAL_DOMAIN}
    ports:
      - "${NGINX_HTTP_PORT}:80"
      - "${NGINX_HTTPS_PORT}:443"
    networks:
      - zhiweijz-network
    depends_on:
      - frontend
      - backend
      - minio
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -f http://localhost/health && curl -k -f https://localhost/health"]
      interval: 20s
      timeout: 15s
      retries: 3
      start_period: 30s

volumes:
  postgres_data:
    driver: local
  minio_data:
    driver: local

networks:
  zhiweijz-network:
    driver: bridge
    name: ${PROJECT_NAME}-network