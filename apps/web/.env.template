# 只为记账前端环境变量配置模板
# 复制此文件为 .env.local 并根据需要修改配置
# 注意：生产环境通过Docker环境变量管理，不使用.env.production文件

# ==========================================
# 基础配置
# ==========================================

# 应用版本信息
NEXT_PUBLIC_APP_VERSION=0.7.2
NEXT_PUBLIC_BUILD_NUMBER=72

# 构建模式配置
BUILD_MODE=web                    # web | mobile
IS_MOBILE_BUILD=false            # true | false
NEXT_PUBLIC_IS_MOBILE=false      # true | false

# 环境配置
NODE_ENV=development             # development | production | test

# ==========================================
# API配置
# ==========================================

# 后端服务器地址（开发环境）
DEV_BACKEND_URL=http://localhost:3000

# API基础URL配置
# 开发环境: 留空使用智能检测（自动使用localhost:3000）
# 生产环境: 设置为完整的API地址
NEXT_PUBLIC_API_BASE_URL=

# 调试版本API配置（可选）
NEXT_PUBLIC_DEBUG_API_BASE_URL=https://jz-pre.jacksonz.cn:4443

# 外部访问域名配置（生产环境必填）
# 用于nginx重定向、iOS快捷指令返回地址、token验证地址等
# 示例: https://your-domain.com:443
EXTERNAL_DOMAIN=https://your-domain.com

# 官方网站地址
NEXT_PUBLIC_OFFICIAL_WEBSITE=https://www.zhiweijz.cn

# ==========================================
# 版本管理配置
# ==========================================

# 版本更新检查功能开关
NEXT_PUBLIC_ENABLE_VERSION_CHECK=true

# 版本检查间隔 (毫秒)
NEXT_PUBLIC_VERSION_CHECK_INTERVAL=86400000

# 自动检查开关
NEXT_PUBLIC_AUTO_VERSION_CHECK=true

# ==========================================
# RevenueCat支付配置（仅iOS使用）
# ==========================================

# RevenueCat iOS SDK API密钥（公开密钥，用于iOS客户端）
# 从 RevenueCat Dashboard → Project Settings → API keys → App specific keys → iOS 获取
# 格式：appl_xxxxxxxxxxxxxxxxx
NEXT_PUBLIC_REVENUECAT_IOS_API_KEY=your_ios_sdk_api_key_here

# 向后兼容的通用API密钥（与iOS密钥相同）
NEXT_PUBLIC_REVENUECAT_API_KEY=your_ios_sdk_api_key_here

# 注意：Android使用单独的第三方支付，不使用RevenueCat
# 注意：Web版本暂不支持支付功能

# ==========================================
# 注意：控制台日志管理已内置
# ==========================================
#
# 应用已内置静默日志管理系统：
# - 默认禁用所有调试日志
# - 无需环境变量配置
# - 开发者可通过控制台命令临时启用调试
#

# ==========================================
# 数据库配置（如果前端需要直接访问）
# ==========================================

# 数据库连接URL（通常由后端处理，前端不需要）
# DATABASE_URL=your_database_url_here

# ==========================================
# 其他配置
# ==========================================

# 禁用Next.js遥测
NEXT_TELEMETRY_DISABLED=1

# ==========================================
# 配置说明
# ==========================================

# 本地开发环境配置说明:
# - NODE_ENV=development (开发模式)
# - NEXT_PUBLIC_API_BASE_URL= (留空，自动使用localhost:3000)
# - DEV_BACKEND_URL=http://localhost:3000 (本地后端地址)

# 生产环境部署说明:
# - 生产环境通过Docker环境变量管理，不使用.env文件
# - 在docker-compose.yml或Docker运行时设置环境变量
# - 日志管理已内置，无需额外配置

# 移动端构建配置:
# - BUILD_MODE=mobile
# - IS_MOBILE_BUILD=true
# - NEXT_PUBLIC_IS_MOBILE=true
